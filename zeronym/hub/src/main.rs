//! Placeholder binary so the crate builds as a bin target while the serving
//! path is still being designed.
fn main() {
    eprintln!("zero-indexer-hub: not yet runnable; the queue and flush are unimplemented");
    std::process::exit(1);
}

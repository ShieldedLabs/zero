# Copy to `.env` to pin the image version used by docker-compose.yml.
# Defaults to `latest` if unset.
#
# Pin to a specific Zero release for reproducible deployments, e.g.:
#   ZERO_IMAGE_TAG=v3
ZERO_IMAGE_TAG=latest
